package tifmo

package knowledge {
	
	abstract class WordInfo extends Serializable {
		def lex: String
		def sign: Boolean
		def isStopwd: Boolean
		override def toString = (if (sign) "" else "~") + lex
	}
}
